version: "3.9"
services:
  n8n:
    build: .
    container_name: n8n-yt-dlp
    ports:
      - "5678:5678"
    volumes:
      - ./n8n_data:/home/node/.n8n
      - ./yt_downloads:/root/yt_downloads
    restart: unless-stopped
